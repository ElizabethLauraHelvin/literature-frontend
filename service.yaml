apiVersion: v1
kind: Service
metadata:
  name: literature-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: literature-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
